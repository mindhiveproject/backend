enum Permission {
  IT
  ADMIN
  SCIENTIST
  MENTOR
  TEACHER
  STUDENT
  PARTICIPANT
}

type Profile {
  id: ID! @id
  username: String! @unique
  permissions: [Permission] @scalarList(strategy: RELATION)
  authEmail: [AuthEmail]!
  authToken: [AuthToken]!
  authInvite: [AuthInvite]! @relation(name:"Owner")
  invites: [AuthInvite]! @relation(name:"Invite")
  results: [Result]!
  studentIn: [Class]! @relation(name:"Studentinclass")
  teacherIn: [Class]! @relation(name:"Classcreator")
}

type AuthEmail {
  id: ID! @id
  email: String! @unique
  password: String!
  resetToken: String
  resetTokenExpiry: String
  profile: Profile!
}

type AuthToken {
  id: ID! @id
  token: String! @unique
  tokenExpiry: String
  profile: Profile!
}

type AuthInvite {
  id: ID! @id
  profile: Profile! @relation(name:"Owner")
  invitedBy: Profile! @relation(name:"Invite")
}

type School {
  id: ID! @id
  title: String!
  description: String
  image: String
  largeImage: String
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
  creator: Profile!
}

type Class {
  id: ID! @id
  title: String!
  description: String
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
  creator: Profile! @relation(name:"Classcreator")
  students: [Profile]! @relation(name:"Studentinclass")
}

type Experiment {
  id: ID! @id
  title: String!
  description: String
  image: String
  largeImage: String
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
  author: Profile!
}

type Result {
  id: ID! @id
  experiment: Experiment! #relationship to Experiment
  user: Profile! #relationship to Profile
  quantity: Int!
  data: String
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
}
